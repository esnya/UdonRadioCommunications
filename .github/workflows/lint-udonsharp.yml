name: UdonSharp Lint

on:
  push:
    branches:
      - main
      - master
      - alpha
      - "copilot/**"
    paths:
      - "Packages/**/*.cs"
  pull_request:
    paths:
      - "Packages/**/*.cs"
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "6.0.x"

      - name: Install UdonSharp Linter
        run: dotnet tool install -g tktco.UdonSharpLinter

      - name: Run UdonSharp Linter
        run: udonsharp-lint Packages
